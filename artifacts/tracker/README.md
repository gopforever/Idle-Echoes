# Melvor EQ2 Tracker

Local tracker dashboard for Melvor EQ2.

## Setup
1. Make sure the API server is running: `cd artifacts/api-server && pnpm dev`
2. Install deps from root: `pnpm install`
3. Start the tracker: `cd artifacts/tracker && pnpm dev`
4. Open http://localhost:5174
